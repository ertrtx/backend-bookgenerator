from google.cloud import storage

currentDir = os.getcwd()
secretPath = os.path.join(currentDir, 'gpt-j','secret')

keyFile = os.path.join(secretPath,'my_keys.json')
emos.environ["GOOGLE_APPLICATION_CREDENTIALS"]= keyFile

def upLoadToBucket(name, prompt):
      client = storage.Client(project_id)
      bucket = client.bucket(bucket_name)
      blob = bucket.blob(name)
      blob.metadata =  {'prompt' : prompt}
      blob.upload_from_filename(name)

      print(
          "File uploaded to ", name
      ) 
